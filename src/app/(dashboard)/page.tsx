// TODO: Dashboard home — summary cards (revenue, orders, views), recent activity feed

export default function DashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">Dashboard</h1>
    </main>
  );
}
