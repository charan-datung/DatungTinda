// TODO: Vision API — accepts image upload, returns AI-generated product title, description, tags

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ message: "Vision AI endpoint — coming soon" });
}
