// TODO: SEO AI — generates meta title, description, and keywords for product/storefront pages

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ message: "SEO AI endpoint — coming soon" });
}
