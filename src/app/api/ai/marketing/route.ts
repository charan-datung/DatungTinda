// TODO: Marketing AI — generates social captions, hashtags, and ad copy from product data

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ message: "Marketing AI endpoint — coming soon" });
}
